# Production environment variables
# This file is used when building the app for production

# API Base URL - Leave empty for production since API is on the same domain
# The backend API and frontend are deployed together on Railway
# API calls will use relative paths (e.g., /api/names)
NEXT_PUBLIC_API_BASE_URL=